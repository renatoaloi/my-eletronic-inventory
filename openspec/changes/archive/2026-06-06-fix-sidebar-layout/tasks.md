## 1. Sidebar CSS Fix

- [x] 1.1 Add `lg:min-h-screen` to the sidebar `<aside>` element to make it fill full viewport height on desktop
- [x] 1.2 Add `lg:relative` to the sidebar `<aside>` element so the version badge has a positioning context on desktop

## 2. Visual Verification

- [x] 2.1 Verify sidebar fills the full viewport height on desktop (≥1024px)
- [x] 2.2 Verify version badge is positioned at the bottom of the sidebar on desktop
- [x] 2.3 Verify mobile sidebar behavior is unchanged (fixed positioning, slide animation, overlay)
- [x] 2.4 Verify main content area layout is unaffected
