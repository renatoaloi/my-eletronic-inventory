# API Path Configuration

This bugfix has no new or modified capabilities — it's purely an infrastructure/configuration change. All existing capabilities remain unchanged in behavior.

The changes are limited to:
- Frontend API base URL configuration
- Dev and production proxy/passthrough configuration

No API contracts, data models, or user-facing behavior changes.
